# Build de produção (o Vite carrega este arquivo em `npm run build`).
# Serve de fallback quando VITE_API_URL não é definido no ambiente do CI/host.
# Pode ser sobrescrito por uma variável de ambiente VITE_API_URL no Cloudflare.
VITE_API_URL=https://gente-gestao-api-sheets.ivanils0n.workers.dev
